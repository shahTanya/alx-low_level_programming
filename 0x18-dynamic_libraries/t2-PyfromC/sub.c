#include "main.h"

/**
 * sub - finds the difference between a and b.
 * @a: a long int.
 * @b: a long int.
 *
 * Return: the difference between a and b.
 */
long int sub(long int a, long int b)
{
	return (a - b);
}
