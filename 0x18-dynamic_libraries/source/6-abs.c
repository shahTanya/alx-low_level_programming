/**
 * _abs - computes the absolute value of an integer
 * @n: integer input
 *
 * Return: absolute value of input
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n == 0)
	{
		return (n);
	}
	else
	{
		return (n * -1);
	}
}
